import Client from './Client';

export const metadata = {
  title: 'Services — Align USA Group',
  description:
    'Three pillars under one delivery model — staffing, software, and consulting — for US companies, run from Houston.',
};

export default function ServicesPage() {
  return <Client />;
}
